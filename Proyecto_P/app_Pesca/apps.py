from django.apps import AppConfig

class AppPescaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_Pesca'  # El nombre debe coincidir con la carpeta de la aplicación
